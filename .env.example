# ShipBest OMS 接口配置（在 OMS 后台的 API 配置里获取）
SHIPBEST_BASE_URL=https://oms.shipbest.com
SHIPBEST_API_ID=
SHIPBEST_ACCESS_TOKEN=
# 设为 1 时不调用真实接口，使用模拟数据（本地试用 / 演示用）
SHIPBEST_MOCK=0

# 后台登录密码 & 会话签名密钥（请改成足够长的随机字符串）
ADMIN_PASSWORD=change-me
SESSION_SECRET=please-change-to-a-long-random-string

# 数据目录（SQLite 数据库和面单文件）
DATA_DIR=./data

# Cookie 只走 HTTPS：1 强制，0 关闭；不填则自动判断（用 http://IP 测试时会自动关闭）
COOKIE_SECURE=

# 设为 1 时首次启动会创建演示客户和演示数据（仅测试用，正式环境不要开启）
DEMO_SEED=0

# 客户忘记密码时发送重置邮件（可选；不配置则由后台客服重置）
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# 系统对外网址，用于邮件里的链接，例如 https://ship.example.com
APP_URL=
